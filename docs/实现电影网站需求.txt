整体风格仿照豆瓣，出设计图；

前端：

1、登录(顶部弹出toast提示)；
2、注册；
3、列表页（菜单列表下拉加载，实现分页）；  完成（但有一个监听obj不触发的bug未解决）
4、详情页面；
 
服务端：

1、实现注册功能；   完成
2、跑定时任务去拉取豆瓣电影数据；
3、实现数据分页加载；     完成
4、所有API都需要带登录态，没有登录态的API，返回跳转code（封装成中间件）；
5、sessionId需要加密；


（1）登录注册加toast效果（封装一个组件）；   完成
（2）表单校验（包括手机号格式、邮箱格式、密码是6-8位、英文和数字混合，不为空，一共五种校验方式）；  完成
（3）开发详情页样式；   完成
（4）完善详情页的数据，可实现列表跳转到详情页的完整链路；  完成
（5）封装电影API都要带登录态的中间件；  完成
（6）实现sessionId加密；
（7）实现跑定时任务去拉取豆瓣电影数据；
（8）解决监听obj不触发的bug；






  